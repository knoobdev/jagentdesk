appId: app.jagentdesk.mobile

---
- launchApp:
    clearState: true
- assertVisible: "Connect to JAgentDesk"
- tapOn:
    id: pair-start-link
- assertVisible: "Pair with JAgentDesk desktop"
- tapOn:
    id: pair-link-input
- setClipboard: ${PAIRING_OFFER_URL}
- pasteText
- tapOn:
    id: pair-link-submit
- extendedWaitUntil:
    visible: "Join your tailnet"
    timeout: 90000
- tapOn:
    id: tailscale-login-authkey-input
- setClipboard: ${JAGENTDESK_E2E_TAILSCALE_AUTH_KEY}
- pasteText
- tapOn: "Auth key"
- tapOn:
    id: tailscale-login-authkey-submit
- extendedWaitUntil:
    visible: "Enter verification code"
    timeout: 90000
- assertVisible:
    id: pairing-code-input
- assertVisible: "Enter the 6-digit code shown in the JAgentDesk desktop connection popup."
- takeScreenshot: tailscale-pair-request-ready
